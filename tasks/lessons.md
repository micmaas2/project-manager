# Lessons Learned

Post-session retrospective notes. Updated after each agent run or user correction.

| Date | Agent | Lesson | Applied To | Confidence | Scope |
|------|-------|--------|------------|------------|-------|
| 2026-04-13 | Builder | **Fail-open auth guards require explicit WARNING logging**: When an auth guard fails open (e.g. TELEGRAM_CHAT_ID=None → allow all), add a logger.warning() so operators can detect misconfiguration in logs. Silent fail-open is a MEDIUM security risk — the permissive mode is invisible. | All future Builder tasks with auth guards |
| 2026-04-13 | Builder | **sys.modules pre-injection pattern for Docker-only packages**: When testing a Python module that imports from a package only available in a Docker image (e.g. src.integration), pre-populate sys.modules with MagicMock entries for all transitive deps, then load the file via `importlib.util.spec_from_file_location()`. Canonical example: `artefacts/task-019/test_auth_guard.py`. | Future Builder tasks testing Docker packages |
| 2026-04-13 | Builder | **Docker COPY src/ layer invalidation is deterministic and fast when deps are cached**: When only source code changes (not requirements.txt), the pip install layer stays cached and only the COPY src/ layer rebuilds. On Pi4 ARM this is ~5s vs 5+ min for a full rebuild. Plan for fast incremental builds when source-only changes. | All future Pi4 Docker build tasks |
| 2026-04-05 | Builder | **Verify tool availability before scripting**: `jq` was not installed at build time. Scripts with external dependencies should check for the tool and emit a clear error — and the MVP template should list runtime prerequisites under `tests_required` or a new `prerequisites` field. | All future Builder tasks |
| 2026-04-05 | ProjectManager | **Execute agents sequentially with queue.json as shared state**: The queue.json file is the effective handoff contract between agents — each agent reads status and assigned_to to know what to do. Keeping it updated after every step prevents context drift in multi-step pipelines. | All future pipelines |
| 2026-04-05 | Tester | **Test with fixtures, not just live data**: Running tests against the real queue.json ties test results to live system state. Always test with at least one controlled fixture (empty queue, seeded queue) to make acceptance criteria provable independently of current system state. | All future Tester runs |
| 2026-04-05 | Builder/Reviewer | **List all acceptance-criteria tools in prerequisites**: If an acceptance criterion says "passes shellcheck", then shellcheck must appear in `mvp_template.prerequisites`. The preflight step catches missing tools before Builder runs — but only for tools that are declared. Undeclared tools fail silently during review. | All future tasks with static-analysis criteria |
| 2026-04-07 | Builder | **Use `re.MULTILINE` when searching multi-line content with `^` anchors**: A regex compiled without `re.MULTILINE` treats `^` as start-of-string, not start-of-line. `_BOLD_RE.search(content)` silently returned no matches across all old-format notes until `re.MULTILINE` was added. Always add this flag when the pattern uses `^`/`$` and the input is multi-line text. | All future Builder tasks using regex on file content |
| 2026-04-07 | Builder | **Tighten old-format detection to require identifying fields, not just a heading**: Detecting old-format notes by `# heading` alone matched Dashboard.md and other plain-Markdown files. Adding a second check (at least one `**Key:** value` bold field present) produced false-positive-free detection. When writing format detectors, require the combination of markers that uniquely identifies the format. | All future migration/detection scripts |
| 2026-04-05 | Builder | **Avoid no-op pipeline stages**: A `sed 's/^//'` (replace start-of-line with nothing) has no effect and adds a pointless pipeline stage. Reviewer caught it but it still shipped. Builder should not emit commands that do nothing — if unsure, test the pipeline without the stage first. | All future Builder script tasks |
| 2026-04-07 | ProjectManager | **Check for artefact directory conflicts when assigning task IDs**: artefacts/task-006/ and artefacts/task-007/ were pre-populated from informally completed tasks. When new tasks were queued with the same IDs, paths clashed. Fix: always audit `ls artefacts/` before assigning the next task ID; use a descriptive suffix (e.g. task-007-gate/) for the new task. | All future PM planning sessions |
| 2026-04-07 | SelfImprover | **Documentation-only tasks pass review cleanest when scoped to the minimum viable diff**: task-006 touched exactly 2 lines in CLAUDE.md and 1 field in queue.json — Reviewer approved on first pass with zero change requests. When a task is read-only / docs-only, resist the temptation to refactor adjacent content; minimal diff = no review cycles. | All future docs/CLAUDE.md tasks |
| 2026-04-07 | Builder | **Add skip-if-missing guards to all mandatory session-start steps**: Step 0b (lessons read) includes an explicit "if file does not exist, skip" guard — preventing a hard failure when lessons.md is absent in a fresh repo clone. Any mandatory workflow step that reads a file should include this guard to make the step safe for new environments. | All future CLAUDE.md workflow step additions |
| 2026-04-07 | SelfImprover | **Artefact path conflicts recur — the CLAUDE.md queue section already documents the fix but it was not followed**: The lesson about `ls artefacts/` before task ID assignment was added to CLAUDE.md in the same session it was needed (task-006, task-007). This suggests the instruction must be surfaced at planning time, not just documented. Consider adding it as a preflight checklist item in the PM planning session step. | All future PM planning sessions |
| 2026-04-07 | SelfImprover | **Keep CLAUDE.md and agent YAML format specs in sync**: task-007-gate added an improvement_proposals.md format block to both CLAUDE.md and self-improver.yaml. Reviewer verified consistency explicitly. Any format or protocol defined in CLAUDE.md must be mirrored exactly in the corresponding agent YAML — and the Reviewer checklist should include a cross-file consistency check for formats that appear in multiple locations. | All future tasks touching agent YAML + CLAUDE.md together |
| 2026-04-07 | Tester | **Map every acceptance criterion to a grep-provable file assertion**: task-007-gate achieved 100% pass with zero rework because each criterion referenced a specific file and a specific section verifiable by inspection. For any doc/config task, translate each criterion into a deterministic file-state assertion before marking tests done. | All future documentation and configuration tasks |
| 2026-04-07 | Builder | **Document the vault-sync gap explicitly and queue a follow-up task**: When a capture path ends in a GitHub repo (not the live Obsidian vault), the limitation must be documented in deploy-notes.md AND a follow-up task queued in backlog.md at build time. Leaving the gap undocumented creates a silent data-loss risk for the user who expects live vault availability. | All future capture / sync tasks |
| 2026-04-07 | Builder | **Stdlib-only is a first-class design constraint for laptop scripts**: capture.py used `urllib`/`base64`/`json` only — no pip installs needed. This removes setup friction and dependency drift. For any script that runs on an arbitrary developer laptop, declare "no external dependencies" as an explicit acceptance criterion in the mvp_template. | All future laptop/cross-platform scripts |
| 2026-04-10 | ProjectManager | **Always `git fetch origin` before reading operational files at session start**: n8n writes `tasks/telegram-inbox.md` directly to `origin/main` via GitHub API. Reading the local file without fetching first silently misses all items committed since the last pull. Fix: `git fetch origin` is now step 0a of the session-start checklist; inbox is read via `git show origin/main:tasks/telegram-inbox.md`, not from the local filesystem. | All future session starts |
| 2026-04-07 | SelfImprover | **Zero reviewer change requests + 100% test pass on first run is achievable with tight scope discipline**: task-008 had no reviewer change requests and 7/7 tests passing. This correlates with an explicit non-scope list, stdlib-only constraint, and two well-bounded capture paths. Tight scope up-front reduces rework — record this as the target baseline for all new tasks. | All future PM planning sessions |

| 2026-04-07 | Builder | **Use `importlib.util.spec_from_file_location` to test hyphenated-filename scripts**: Python cannot `import migrate-vault` directly. Always use `importlib.util.spec_from_file_location("module_name", "/path/to/script.py")` when the script filename contains hyphens. This pattern is required for all Pi4 migration/utility scripts with hyphenated names. | All Builder tasks producing hyphenated-filename scripts |
| 2026-04-11 | Builder | **Bundle all correlated tracking writes into a single feature-branch commit**: Skill files that commit to multiple tracking files (queue.json + backlog.md + kanban.md + epics.md) must include all writes in one commit — partial commits leave tracking files out of sync if the session is interrupted. | task-013 |
| 2026-04-07 | Builder | **Thread `--dry-run` through every file-writing function as an explicit parameter**: Migration scripts must pass `dry_run: bool` all the way down to the function that opens files for writing. Handling dry-run only at the top-level caller risks accidental writes from helper functions invoked in non-obvious paths. | All future Builder migration/transformation scripts |
| 2026-04-07 | Security | **Always block private IP ranges in outbound HTTP fetchers**: Scripts that fetch arbitrary URLs from user-controlled data (e.g. vault notes) must block `localhost`, `127.x`, `10.x`, `192.168.x`, and `172.16-31.x` before issuing `requests.get`. Use a hostname-check function pre-request; document the private-IP check in the MVP template security section for any task with outbound HTTP. | All future Builder tasks with outbound HTTP |
| 2026-04-07 | ProjectManager | **Rejected proposal P5 (task-006)**: "add tooling/testing/task-design wording to step 0b" — already present in CLAUDE.md. Check current file state before writing proposals that target existing text. | SelfImprover: read target section before proposing additions |
| 2026-04-07 | ProjectManager | **Rejected proposal P6 (task-007-gate)**: "ls artefacts/ preflight as numbered step" — duplicate of P4 (task-006-lessons Proposal 1), which was approved and applied in the same session. Cross-check proposals across all tasks in a session before presenting to user. | SelfImprover: deduplicate across concurrent task proposals |
| 2026-04-07 | Builder | **Mandate slug validation + path-traversal guard for any task writing files at dynamic paths**: task-004 correctly implemented `[a-z0-9-]+` slug validation and a `filepath.startsWith(VAULT + '/')` guard, but these were not required by the MVP template security section — the Builder added them from experience. For any task where a user-controlled value (topic, category, tag) is used in a file path, the MVP template must explicitly require: (1) allowlist or regex validation before path construction, and (2) a startsWith-vault check on the final path before any write. | All future Builder tasks writing files at dynamic paths |
| 2026-04-07 | Builder | **Keep LLM prompt guidance and code validation in sync**: task-004 told the model "return a single lowercase slug word (no hyphens)" but the Parse node accepted hyphens via `[a-z0-9-]+`. The mismatch is benign here (implementation is more permissive), but inconsistency between prompt and validator is a latent defect: if the model follows the prompt strictly, valid hyphenated slugs may be silently sanitised. Whenever a validation pattern is defined in code, the corresponding LLM prompt must state the same allowed character set. | All future tasks with LLM-generated structured fields |
| 2026-04-07 | Builder | **Telegram confirmation messages must echo the fully-resolved path, not just the filename**: task-004's confirmation replied with only `title` and `filename`, omitting `Category/Topic/filename`. When a workflow resolves a multi-level path, the confirmation should reflect the full path so the user can verify the correct folder was used without opening Obsidian. Apply this to any n8n capture workflow that writes to a nested directory. | All future n8n capture / file-write workflows |
| 2026-04-07 | code-quality-reviewer | **YAML injection in note-writing workflows is a HIGH severity finding — require explicit escaping in the MVP template**: task-009 correctly escaped `title` and `from` fields (tests 9 and 10 pass) but the MVP template security section did not mandate it. The code-quality-reviewer caught the gap at HIGH severity; the Reviewer flagged it as a non-blocking observation. For any task writing externally-sourced values into YAML frontmatter, the MVP template `security_arch_impact` must explicitly require YAML-escape or JSON-encode before interpolation. | All future note-capture and file-write tasks |
| 2026-04-07 | Builder | **Null-guard external IDs used in downstream steps — require it in the MVP template**: task-009 implemented a `gmailData.id` null check (test 11 passes) from experience, not from a template requirement. If the MVP template had not been updated and a future Builder omitted this guard, emails would be written to vault but never marked as read — a silent operational failure. The MVP template `security_arch_impact` must mandate non-null validation for any external ID used in a subsequent step. | All future n8n / API integration tasks |
| 2026-04-07 | Builder | **Newline stripping alone is insufficient — mandate full control-character sanitisation for external string fields**: task-009's `from` field stripped newlines but not other ASCII control characters (null bytes, carriage returns, etc.). Externally-sourced strings written to YAML or Markdown should have all control characters (0x00–0x1F, 0x7F) stripped or escaped, not just `\n`. Add this as a standard requirement in the MVP template for any task handling external string data. | All future Builder tasks writing external data to files |
| 2026-04-08 | Builder | **n8n credential IDs in workflow JSON templates are placeholders — they must be replaced with real DB IDs after import**: n8n resolves credentials by internal UUID, not by name. A workflow imported with a placeholder ID (e.g. `anthropic-cred`) will silently fail all API calls because the credential lookup returns nothing. Symptoms: auth errors or empty responses in HTTP Request nodes, with downstream nodes never executing. Fix: after deploying any workflow, verify each node's credential ID matches a real credential in n8n (`n8n export:credentials --all`). For workflows that share credentials across re-imports, hard-code the live ID in the canonical source file and document it. | All future n8n workflow deployments |
| 2026-04-10 | ProjectManager | **Git-only tasks need a verification.md artefact — "just a merge" is not exempt from traceability**: task-003 had no artefact directory, leaving SelfImprover with nothing to analyse. Fix: CLAUDE.md step 4 now mandates a `verification.md` for every task (commands run, output, PASS/FAIL per criterion); queue.schema.json enforces non-empty `artefact_path` before `status: done`. | All future git-only or ops tasks |
| 2026-04-10 | Builder | **Cron-deployed shell scripts require a hardening checklist in the MVP template**: task-011 needed 2 CRs and 4 code-quality fixes — all addressing cron-specific gaps (log writability guard, flock concurrency lock, explicit SSH identity, log rotation marked required). None were in the MVP template or acceptance criteria, so Builder omitted them and Reviewer caught them in pass 2. For any task deploying a shell script under cron or systemd, the MVP template must include: (1) log file writability guard, (2) flock concurrency lock, (3) explicit SSH/auth identity export, (4) log rotation as a required deploy step. | All future Builder tasks producing cron/daemon shell scripts |
| 2026-04-10 | Tester | **`re.MULTILINE` and `re.DOTALL` conflict when `$` is used as a terminator in lazy matches**: combining both flags causes `$` to match end-of-line rather than end-of-string, so a lazy `(.*?)(?=...\|$)` stops at the first newline even though `re.DOTALL` allows `.` to cross line boundaries. This silently truncates multi-line captures to their first line. Fix: drop `re.MULTILINE`; replace `^` with `(?:^\\n)` and trailing `$` with `\\Z`. Reviewer and code-quality-reviewer both missed this — it was only caught by a dedicated multi-line fixture in the test suite. Always add a multi-line fixture when reviewing any `re.DOTALL` regex that uses `$` as a stop anchor. | All future Builder and Reviewer tasks using regex to parse structured LLM output |
| 2026-04-11 | Architect | **Architecture reviews expose systemic SPOFs missed by unit testing**: task-014 discovered that Pi4 is a single point of failure for both pensieve and mas_agent through explicit dependency mapping (not through test failures). When designing multi-system workspaces, reserve time in architecture reviews to identify hardware/infrastructure SPOFs. | All future Architect tasks reviewing multi-system deployments |
| 2026-04-11 | ProjectManager | **Advisory-only budget enforcement creates silent overspend risk**: project_manager declares a 500k token cap in CLAUDE.md and mas_agent has budget config fields, but neither system blocks execution when budget is exceeded — check_budget() is report-only. Config-based limits without runtime enforcement are false signals of control. Always pair declared limits with blocking guards. | All future token budget policies |
| 2026-04-11 | ProjectManager | **Unshared observability across independent systems means three separate failure modes with no cross-alerting**: pensieve (n8n), mas_agent, and project_manager each run independently with no shared health monitoring. A single lightweight health-check workflow would cover multiple systems simultaneously. When deploying multiple services, invest in a shared monitoring plane early. | All future multi-system deployments |
| 2026-04-11 | ProjectManager | **Architecture review tasks must include an explicit acceptance criterion to convert all NEW proposals into backlog items**: task-014 produced 16 proposals across 3 systems; SelfImprover only surfaced 3 PM-internal ones into improvement_proposals.md. The remaining 13 sat in architecture-review.md undiscovered until the next planning session. Fix: add "all NEW proposals registered as BL items in backlog.md" to the Definition of Done for any architecture/research task. | All future architecture review tasks |
| 2026-04-11 | SelfImprover | **pytest tmp_path is outside workspace root — fixture files for scripts with _safe_path() must live under artefacts/<task-id>/_fixtures/**: All 9 tests initially failed because tmp_path resolves to /tmp, which is outside the workspace root checked by _safe_path(). Place fixture files under artefacts/<task-id>/_fixtures/ for any script that validates paths against a workspace root. | task-016 |
| 2026-04-11 | SelfImprover | **Always use read_text(encoding="utf-8") + catch UnicodeDecodeError when reading JSON files**: stdlib default encoding varies by platform (locale-dependent on Linux/macOS). Omitting encoding= is a latent bug on non-UTF-8 systems. The code-quality-reviewer caught this pattern in task-016 — standardise it for all file reads in Python scripts. | task-016 |
| 2026-04-11 | Builder | **Include BL-NNN in every queue.json task title**: pm-priority.py extracts BL-NNN from the task title via regex to look up priority in backlog.md. Tasks with only S-NNN story IDs have no BL cross-reference and rank as lowest priority by default. Always include `(BL-NNN)` in the title when queuing a task. | All future PM planning sessions |
| 2026-04-13 | Builder | **`bash -n` does not work for Python scripts — use `python3 -m py_compile` instead**: Running `bash -n cross-kanban.py` exits 2 with a spurious syntax error (bash cannot parse Python). Always use `python3 -m py_compile <script>` for Python syntax pre-submission checks. | All future Builder tasks producing Python scripts |
| 2026-04-13 | Builder | **Explore subagents cannot write files — apply their output directly**: When DocUpdater/docs-readme-writer are spawned as Explore-type agents, they correctly identify required changes but cannot write files. The parent agent must apply the edits after receiving the agent's output. Document this in orchestration notes so future sessions don't wait for the agent to write. | All future Doc stage pipeline runs |
| 2026-04-13 | Builder | **`docker compose up -d --build <service>` rebuilds all depends_on services too**: building `mas-frontend` also triggered a full `mas-backend` rebuild (5+ min Python pip install on Pi4 ARM). When targeting a specific service, plan for the full dependency build chain time. | All future Pi4 Docker rebuild tasks |
| 2026-04-13 | Builder | **`/opt/mas` git repo is root-owned — use `sudo git -c user.name=... -c user.email=...`**: The Pi4 /opt/mas repo was cloned as root; `pi` user cannot write to .git. All git operations need `sudo git -C /opt/mas` with explicit `-c user.name='Michel Maas' -c user.email='michel@femic.nl'` since root has no git config. | All future mas_personal_assistant git tasks |
| 2026-04-14 | Builder | **Never let LLMs infer dynamic facts (today's birthdays) without verified external data**: LLM hallucinated birth dates by reusing today's date. Always fetch and pass verified candidates before prompting. | All future LLM-generated daily/dynamic-fact tasks |
| 2026-04-14 | Builder | **Wikipedia API returns 200+ entries newest-first; intellectuals require explicit keyword sort**: Default first-10 yielded only modern athletes. Parse all entries, keyword-match intellectuals, sort intellectuals-first + oldest-first, take top N. | All future Wikipedia API born-today integrations |
| 2026-04-14 | Builder | **`description` fields from external APIs need control-char sanitization before LLM prompt injection**: Security review caught unsanitized Wikipedia `description` fields embedded in prompts. Strip 0x00–0x1F for any external string used in a prompt or file write. | All future tasks fetching external API data into LLM prompts |
| 2026-04-14 | Builder | **Use line-anchored ERE (`^\*\*Status\*\*:`) not plain grep for improvement_proposals.md scan**: Plain `grep -l "REQUIRES_HUMAN_APPROVAL"` produces false positives from body text and test assertion files. `grep -lE "^\*\*Status\*\*: REQUIRES_HUMAN_APPROVAL"` anchors to the Status field line. | pm-propose scan, CLAUDE.md scanning note |
| 2026-04-14 | Builder | **Skill-creator plugin provides a rigorous eval loop for PM skills**: Our PM skills have no evals or trigger optimization. Use skill-creator (eval test cases + baseline subagents + description optimizer) before shipping new skills or after identifying skill failures. | BL-074, future skill authoring |
| 2026-04-14 | Builder | **session-report gives token observability with zero custom tooling**: token_log.jsonl is text-only; session-report generates interactive HTML from transcripts. Lowest-effort observability improvement available. | BL-073, /pm-status wiring |
| 2026-04-14 | SelfImprover | When removing nodes from a parent n8n workflow, search ALL remaining node parameters for `$('RemovedNodeName')` references — downstream reply/format nodes often reference earlier nodes by name. Fix before import. | task-029 |
| 2026-04-14 | SelfImprover | `n8n import:workflow` always deactivates the workflow; always follow with `publish:workflow` + `docker restart n8n` to reactivate. | task-029 |
| 2026-04-14 | SelfImprover | `n8n export:workflow --output path` writes inside the container; use `docker cp` to extract, or pipe stdout directly — `docker cp` from Pi4 host requires a second SSH hop which can fail silently. Prefer piping stdout. | task-029 |
| 2026-04-14 | SelfImprover | `n8n publish:workflow` does NOT activate a workflow — it only sets the published version. Sub-workflows called via Execute Workflow node require `active: true`. Use REST API: `curl -X POST http://localhost:88/api/v1/workflows/{id}/activate -H 'X-N8N-API-KEY: ...'`. API key is at `/opt/n8n/api-key` on Pi4. | task-029 |
| 2026-04-15 | SelfImprover | **`n8n export:workflow --all` only exports non-archived workflows — deleted workflows disappear from the count**: After `DELETE /api/v1/workflows/{id}`, the workflow is soft-archived and excluded from export. A count drop in the export is expected and correct — not data loss. Always commit the pre-deletion backup before deleting, then confirm the post-deletion export count matches (before − deleted). | All future n8n cleanup / deletion tasks |
| 2026-04-15 | Builder | **For operational cleanup tasks, pre-condition backup commit is a hard gate — not optional**: The security_arch_impact note in task-031's MVP template specified "export all workflows to git before deleting". The Reviewer explicitly checked this as the first line of the review checklist. For any destructive n8n/Pi4 operation, the backup commit must appear in git history before the first DELETE call. | All future n8n operational tasks |
| 2026-04-14 | SelfImprover | n8n JS task runner (2.x) disallows `require('path')` in Code nodes — use a manual `normalizePath()` function instead of `path.resolve()`. `require('fs')` is still allowed. | task-029 |
| 2026-04-14 | SelfImprover | n8n sub-workflow deploy checklist (confirmed working): 1) import:workflow, 2) activate via REST API POST /api/v1/workflows/{id}/activate, 3) no path module in Code nodes. All three required for a functional sub-workflow. | task-029 |
| 2026-04-14 | Builder | **Index-based array pairing must have a length equality guard**: when pairing two arrays by index (e.g. exec items + workflow items from different nodes), assert `a.length === b.length` first; return `[]` on mismatch to stop the chain safely rather than silently mis-pair items. | task-030, all n8n fan-out workflows |
| 2026-04-14 | Builder | **HTTP Request nodes calling localhost must set `"timeout": 10000`**: n8n default is 300s; a hung local API (e.g. during n8n restart) stalls the workflow for 5 min. 10s is generous for Pi4 localhost calls. | task-030, all n8n HTTP nodes |
| 2026-04-14 | Builder | **n8n workflow self-exclusion: use UUID (primary) + name (fallback)**: name-only exclusion breaks if the workflow is renamed post-deploy. UUID is immutable across rename; name is readable. Combine both with `&&`. | task-030, all n8n health-monitor workflows |
| 2026-04-14 | SelfImprover | **n8n Code node returning `[]` stops the downstream chain** — no IF/Switch node needed. Use this to gate on guard conditions (night hours, length mismatch, empty results). Idiomatic and eliminates conditional branches. | task-030, all n8n Code node patterns |
| 2026-04-15 | code-quality-reviewer | **Secret rotation runbook rollback sections must include time-to-restore estimate**: operators deciding escalation need a worst-case time figure. One sentence ("Estimated time-to-restore: N-M minutes") is sufficient — without it, the rollback section is incomplete for on-call use. | task-032, all future credential-rotation runbooks |
| 2026-04-15 | code-quality-reviewer | **When docs recommend a "preferred" format, ALL code examples must use that format**: task-032 recommended fine-grained PATs but the bash snippet showed only classic `ghp_` prefix. Mismatches between recommendations and examples confuse first-time operators. | task-032, all future runbooks with multi-option guidance |
| 2026-04-16 | ProjectManager | **Phase gate announcements in /pm-start must end with an explicit yes/no approval question**: pm-start announced EPIC-003 phase gate reached but never asked "approve? (yes/no)" — the user had no clear way to respond. Fix: when a phase gate is detected, end the section with an explicit question before printing the summary. | pm-start skill, all future phase gate checks |
| 2026-04-16 | Builder | **Telegram backlog routing requires exact prefix `BACKLOG: ` (uppercase + colon + space) — silent drop on mismatch**: user sent `backlog` without colon; n8n workflow did not match and silently dropped the message. Fix: update n8n Telegram capture workflow to match case-insensitively and accept `backlog` or `backlog:` variants. | n8n Telegram capture workflow, BL-084 sprint |
| 2026-04-16 | SelfImprover | **Obsidian "missing files" should be triaged on Pi4 first before assuming n8n failure**: run `ssh pi4 "find /opt/obsidian-vault -name '*.md' -newer /opt/obsidian-vault -maxdepth 3 | sort | tail -5"`. If files are present on Pi4 but not in the app, the issue is Obsidian app sync (fix: delete + re-add vault). Only escalate to n8n debugging if Pi4 vault is actually empty. | task-033, future pensieve debugging |
| 2026-04-16 | ProjectManager | **pm-plan must define a new epic before queuing tasks when the previous epic is done**: when all stories in the current epic are done, do not reuse that epic_id for new tasks — create EPIC-N+1 with a clear goal and stories first, then assign the new tasks to it. Defaulting to the last epic silently misattributes work. | pm-plan skill, epics.md, all future sprint planning |
| 2026-04-17 | SelfImprover | **Research tasks with large source material require an explicit second exploration pass**: first pass found 7 patterns (BL-085–091); second pass found 4 more (BL-092–095) by fetching SKILL.md content files not reached initially. Without a mandatory second round, first-pass results feel complete but miss 30–40% of applicable patterns. | task-034, all future research/explore tasks |
| 2026-04-17 | SelfImprover | **Document-refactoring tasks require a pre-move checklist to prevent silent content drops**: task-037 moved ~5,800 chars from CLAUDE.md to docs/n8n-deployment.md; 5 actionable instructions were initially dropped (CQR confidence 97). Fix: before moving a section, list every sentence and verify it appears in either the destination doc or is explicitly kept in source — prevents silent losses and reduces CQR rework cycles. | task-037, all future docs-extraction/migration tasks |
| 2026-04-18 | Builder | **Cost calculations in design docs must state explicit input/output token split assumptions with a footnote**: task-039 review found the savings figures implied a methodology without stating it. Without a footnote naming the per-token prices and the assumed input/output split (e.g. 3,500/1,500 tokens), figures cannot be independently verified or recalculated when pricing changes. | All future cost/model-routing design tasks |
| 2026-04-18 | Builder | **Caching savings must be stated as a range (lower/upper bound) not a single figure**: task-039 Reviewer F2 (conf 85): the 5-minute cache TTL means cross-session calls miss cache entirely. Presenting only the upper bound ($18.59/1,000 runs at ~100% hit rate) overstates savings and can skew prioritisation. Always add a lower-bound scenario (e.g. 1 cache hit per 3-call session = ~33% hit rate → $6/1,000 runs). | All future caching-benefit analyses |
| 2026-04-18 | SelfImprover | **/compact placement scope discipline: define the minimum viable set of boundaries per task, not the exhaustive set**: task-038 added /compact at Builder→Reviewer and Reviewer→Tester only — the two highest-context-cost boundaries. The Tester→DocUpdater and DocUpdater→SelfImprover boundaries were identified as valid future additions but excluded from this task's scope by acceptance criteria. Tight scope produced a clean first-pass review with no rework. | pm-run skill, all future context-management tasks |
| 2026-04-18 | SelfImprover | **Add a re-anchoring instruction immediately after each /compact call**: after /compact the agent's working context is reset and the current task_id, artefact_path, and pipeline position are not automatically restored. A brief re-anchor instruction ("re-read queue.json for task_id and confirm artefact_path before proceeding") prevents the next stage from starting in an ambiguous state. | pm-run skill, all future skills that invoke /compact |
| 2026-04-19 | SelfImprover | **Cost projections for downstream agents must use actual token_log.jsonl data, not assumed token counts**: task-039 estimated Tester savings at $0.024/run based on an assumed 5,000 tokens; actual logs showed 1,500 tokens → real savings $0.0066/run (3.6× lower). When a planning task projects savings for another agent, verify the assumed token count against logs before publishing — or explicitly flag it as unverified with a note to correct after first actual run. | task-039, all future cost-projection tasks |
| 2026-04-19 | SelfImprover | **Built-in subagent models are fixed by type and cannot be overridden via YAML — audit methodology must treat them separately**: the `Agent` tool has no `model` parameter, so built-in agents (code-quality-reviewer, docs-readme-writer, revise-claude-md) cannot be downgraded or upgraded through config. Model governance for built-ins is documentation-only. Audits that treat built-ins identically to YAML agents will draw incomplete conclusions about actionable changes. | task-040, all future model governance audits |
| 2026-04-19 | SelfImprover | **When pipeline cost is heavily dominated by one agent (PM Opus at 89.3%), model-swap optimisations on other agents have diminishing returns**: eliminating all non-PM Sonnet usage saves only 10.7% of total cost. Focus cost-optimisation work on the dominant agent (PM prompt compression, prompt caching) rather than chasing small savings on already-cheap pipeline stages. Use a pipeline cost baseline table to identify where effort is worth spending. | task-040, all future cost-optimisation tasks |
| 2026-04-19 | SelfImprover | **Haiku version-string pinning in agent YAMLs creates silent drift risk when CLAUDE.md states only the generic model name**: `doc-updater.yaml` and `self-improver.yaml` use `claude-haiku-4-5-20251001` (date-pinned), but CLAUDE.md policy says `Haiku 4.5`. There is no policy for when to update the pin or what triggers a version bump. When a newer Haiku 4.5 patch is released the YAMLs silently stay on the old pin while docs remain vague. Fix: CLAUDE.md should document the pinning strategy and the trigger for updating it ("update YAMLs when a new Haiku 4.5 patch is released; both YAMLs must be updated together"). | task-040, all future model version governance |
| 2026-04-19 | SelfImprover | **CLAUDE.md recovery rules are retrospective root-cause signals — their presence proves a failure mode occurred even without a lessons.md entry**: The `ExitPlanMode mid-skill recovery` rule in CLAUDE.md names `/pm-propose` as a concrete example, proving plan-mode interruptions happened in practice. When a recovery rule exists in CLAUDE.md for a skill, treat it as evidence of a real past failure and fix the root cause (not just the recovery path). Check for recovery rules before assuming a flow is friction-free. | task-041, all future skill evaluations |
| 2026-04-19 | SelfImprover | **Execution-only skills must carry an explicit plan-mode opt-out directive — the "ALWAYS enter plan mode" mandate applies textually to any flow with 3+ steps, including deterministic pipelines**: /pm-run has 5+ pipeline stages and no ambiguity (task is fully specified in queue.json), yet the general "plan first" rule textually qualifies it for plan-mode activation. Without an explicit opt-out instruction in the skill file, spontaneous plan-mode activation can interrupt automated execution mid-pipeline. Fix: add "do not enter plan mode" to any skill that is execution-only and has no scoping ambiguity. | task-041, pm-run.md, all future execution-only PM skills |
| 2026-04-19 | SelfImprover | **M-1 label-directive consistency is not covered by model-tier M-1 checks — a separate label audit is required**: task-042 found that `tester.yaml` labelled outputs `[Sonnet]` (pre-change) while the CLAUDE.md Label bullet listed only `[Sonnet]` and `[Opus]`. Haiku agents (doc-updater, self-improver) had already been using `[Haiku]` in practice but the governance bullet omitted it. M-1 checks verify model tier names match; they do not verify that the Label directive in each YAML appears in the CLAUDE.md Label bullet. When adding or changing any agent's model tier, also update the CLAUDE.md Label bullet to include that tier — treat the Label bullet as a second M-1 mirror, checked alongside the model-pin table. | task-042, all future model-tier changes |
| 2026-04-19 | SelfImprover | **Date-pinning rationale for Haiku is build-count-driven, not arbitrary — document the decision trigger**: `claude-haiku-4-5-20251001` carries a date suffix because multiple Haiku 4.5 patch builds co-existed; Opus 4.6 and Sonnet 4.6 have single authoritative builds and need no date suffix. Without this rationale in decision.md or CLAUDE.md, future agents may add spurious date suffixes to Sonnet/Opus (noise) or strip the Haiku pin (regression). Pinning strategy decisions must state the specific trigger for adding a date suffix ("multiple patch builds exist") so successors can apply the same rule without guessing. | task-042, all future model version pin decisions |
| 2026-04-19 | SelfImprover | **PreToolUse blocking hooks are categorically different from review-phase tools — enforce security at edit time, not review time**: `security-guidance`'s exit-code-2 blocking mechanism prevents dangerous patterns (eval, os.system, innerHTML, pickle) from ever entering the codebase. Review-phase tools (Reviewer, code-quality-reviewer) catch issues *after* code is written; a PreToolUse hook *prevents* writing them. When evaluating security tooling, prefer PreToolUse hooks for known high-severity pattern classes — they add a control layer no review tool can replicate. | task-043, BL-101, all future security tooling decisions |
| 2026-04-19 | SelfImprover | **The pm-* skills have no formal evals — their triggering accuracy has never been measured**: skill-creator deep-dive (task-043) revealed that all 7 pm-* skills (`pm-start`, `pm-run`, `pm-status`, `pm-plan`, `pm-propose`, `pm-close`, `pm-lessons`) were written without test cases or description optimization. Skills without evals can silently undertrigger (the skill doesn't fire when expected) or overskip steps without a measurable baseline to detect the regression. This differs from the general skill-creator lesson (BL-074): BL-074 is about new skills; this finding is that *existing production skills* have no coverage. Register formal eval work as BL-103 (already queued). | task-043, BL-103, all future skill authoring and audit sessions |
| 2026-04-21 | code-quality-reviewer | **CQR catches silent cost-accounting corruption that Reviewer grades as low-confidence (below fix threshold)**: task-044 Reviewer rated the cost_tracker.py pricing gap at confidence 45 (below the ≥80 fix threshold) because the old model was also absent — classifying it as a pre-existing gap, not a regression. CQR independently analysed the fallback logic, determined the ~20× underestimate was a data corruption path (not just a missing entry), and rated it confidence 92 (BLOCKING). The same finding, different severity. Running CQR in parallel with Reviewer is not redundant: CQR applies technical impact analysis that changes the confidence grade. Never suppress CQR on config-only or migration tasks — pricing/auth/logging layers hide silent-corruption risks that are invisible to scope-scoped review. | task-044, all future model upgrade and config-change tasks |
| 2026-04-21 | Tester | **Document the pre-existing test failure baseline before starting any task on an external repo**: task-044's test suite had 14/148 pre-existing failures (expired OAuth, API mismatches, missing DB tables) that existed before the task branch was created. Without a baseline snapshot, the Tester must reconstruct history from git logs to prove the failures predate the task — this added analysis overhead. Fix: for tasks targeting an external repo (Pi4 MAS, pensieve, etc.), capture a baseline test run result as the first step and store it in the task artefact (e.g. `artefacts/<task-id>/baseline_test_run.txt`). The Tester then compares against the baseline, not against 100%. | task-044, all future tasks on Pi4/MAS/pensieve repos |
| 2026-04-21 | Builder | **Model upgrade tasks must include a cost_tracker/pricing-table check as a mandatory checklist item**: task-044 required a second commit (`c4cf82f`) solely to add `claude-sonnet-4-6` pricing after CQR caught the gap. The fix was two lines and trivial — but it was missed in the initial build because there was no checklist item prompting the Builder to verify the pricing table. For any task that changes or adds an LLM model string, the MVP template definition of done must include: "verify model string appears in cost_tracker (or equivalent pricing table); if absent, add pricing entry before closing the task." | task-044, all future model string upgrade tasks |
| 2026-04-27 | Reviewer | **Guard calls must carry explicit scope-level comments to prevent refactoring into incorrect scopes**: task-045 Reviewer F-1 (confidence 85) identified a maintenance trap: if `_check_budget(model)` is later moved inside the try/except block, `BudgetExceededError` would be caught and enforcement silently broken. The fix: a one-line comment at the call site explicitly stating it must remain outside the try block. For any enforcement or safety gate, the Builder must add a comment naming the specific control-flow contract the guard enforces. | All future Builder tasks implementing enforcement gates |
| 2026-04-27 | code-quality-reviewer | **Default fallbacks with `or` short-circuit defeat zero-disable intent — use explicit None checks**: task-045 CQR Finding 4 (confidence 68) caught `budget_usd = config.value or 10`, which treats both `None` and `0` as "use fallback". A user setting budget to `0` to disable enforcement instead triggers the fallback and silently enables it. Replace `or X` with `if ... is not None else X` to preserve the zero-as-disable signal. | All future config-driven enforcement and feature-flag tasks |
| 2026-04-27 | SelfImprover | **Reviewer confidence 85 (above 80-confidence loop threshold) requires Builder rework even for subtle maintenance traps**: task-045 had zero functional bugs but a single Reviewer finding at confidence 85 triggered a Builder loop. The finding was non-obvious (refactoring a guard out of scope) but highly credible. Confidence ≥80 is the correct threshold for loop triggering; it correctly caught a real, addressable maintenance risk before it caused an incident. | All future Builder → Reviewer pipelines |
| 2026-04-27 | SelfImprover | **Builder must mandate `os.environ["KEY"]` with KeyError handler instead of `os.environ.get("KEY", fallback)`**: task-046 CQR F1 (conf 98) caught a live Telegram bot token hardcoded as the default fallback; Reviewer F1 (conf 92) identified the same issue with KUMA_ADMIN_PASS. This is the second consecutive task (task-044/045, task-046) where credentials appeared as literal fallback defaults. The anti-pattern is `os.environ.get("CRED", "live_value")` — semantically this claims "if the env var is unset, use this hard-coded value", which is a credential storage anti-pattern. The correct pattern is `os.environ["KEY"]` with a try/except that logs and exits — it explicitly fails when the credential is missing, not silently using a fallback. Add a mandatory Sonnet Builder checklist item: "For any script reading a credential (token, password, key) from env, use `os.environ["KEY"]` not `get()` with a fallback; exception handler must log and exit." Document in CLAUDE.md builder.yaml. | All future Builder tasks with credential handling |
| 2026-04-27 | SelfImprover | **CQR found hardcoded credentials in BOTH code (setup script) and docs (build_notes.md) — enforce a blanket rule on artefact files**: task-046 CQR F1 (conf 98) + F3 (conf 90) caught credentials in two artifact files. The build_notes.md credential exposure was easier to miss (plain-text table cell) than code, yet equally damaging if artefacts are later shared or pushed to a public remote. Add a pre-merge check: *any file in artefacts/ matching credential patterns (`MasAdmin\d+`, `\d{10}:[A-Za-z0-9_-]{35}`, etc.) fails pre-commit*. If building outputs that contain credentials for demonstration, redact them before committing artefacts. | All future tasks with manual setup steps documented in build_notes |
| 2026-04-27 | SelfImprover | **Library API contracts are implicit and unmaintained — document when a library feature is unavailable**: task-046 build_notes.md line 56 notes that `uptime-kuma-api` v1.2.1 silently drops the `keyword=` parameter at runtime. The comment is documented but CQR F4 (conf 70) flagged the misleading on-line code comment ("Expect `{"status":"healthy"}` in body") that contradicts actual runtime behaviour. When a library version has a silent limitation, the comment in code must state the current version and the limitation explicitly ("v1.2.1 does not persist keyword; HTTP 200 check only"). Document versioned behavior gaps to prevent regressions when the library is upgraded. | All future Builder tasks using versioned libraries with known gaps |
| 2026-04-28 | SelfImprover | **PreToolUse blocking hooks cause a feedback loop when writing artefacts containing blocked patterns**: task-047 had the hook fire on its own build_notes.md during installation (the file contained `exec(` as documentation). This is operationally correct — the hook is working — but it highlights a real risk: artefacts that document security patterns often contain examples of those patterns. Builder installing a security hook should test with benign payloads first to verify hook registration, then write artefacts that may contain blocked examples, with a documented override (e.g. `ENABLE_SECURITY_REMINDER=0`) if needed. This pattern applies to any PreToolUse guard: test registration before triggering it. | All future security hook or blocking-tool installations |
| 2026-04-28 | SelfImprover | **Copy external hook/script dependencies locally before patching — enables project control and eliminates path fragility**: task-047 initially registered the security hook via an absolute path to the plugin marketplace `/root/.claude/plugins/...`. When CQR identified improvements needed (broad `exec()` substring match, state file permissions), the solution was to copy the hook to `hooks/security_reminder_hook.py` and patch locally. This made the hook git-tracked, eliminates path-drift on upgrades/migration, and proves the pattern for any marketplace utility or hook. When installing external security or utility tools, copy locally first. | All future security hook or marketplace utility installations |
| 2026-04-28 | SelfImprover | **Session-dedup state files with predictable names (session_id="default" fallback) enable silent bypass of security warnings — explicit None check required**: task-047 CQR Finding 2 (conf 82) identified that missing session IDs fall back to `"default"`, allowing a single file `~/.claude/security_warnings_state_default.json` to pre-populate and disable all warnings. The fix (state file `0o600` permissions + fallback removal/explicit session ID handling) was applied during testing. When any tool uses session state to track one-time warnings, avoid predictable IDs and default values — they become attack surface. Explicit None + error fallbacks are safer than string defaults. | All future Builder tasks with session-aware warning deduplication |

| 2026-04-28 | Security | **Reviewer must NEVER embed actual credential values in review findings — use <REDACTED>**: task-046 Reviewer correctly identified a hardcoded Telegram token as F1 (conf 92) and quoted the original bad code verbatim in review.md. That review commit (e9ca862) is exactly what GitGuardian detected. The original vulnerable code can be reconstructed from `git diff`; the live token cannot be un-leaked. In all review/doc artefacts, replace actual credential values with `<REDACTED>` — even in "here is the bad example" code blocks. | All future Reviewer and CQR tasks |
| 2026-04-28 | Security | **os.environ.get() with a non-empty secret fallback is a blocking security finding — now enforced by PreToolUse hook**: task-046 Builder used `os.environ.get("TELEGRAM_BOT_TOKEN", "<live-token>")` as a convenience default. This is categorically unsafe — a silently missing env var uses the hardcoded credential without warning. The security hook (task-047) now blocks this pattern at edit time. Builder must always use the fail-fast pattern (`os.environ.get()` + explicit None-check + `sys.exit(1)`). | All future Builder tasks loading secrets from env vars |
| 2026-04-28 | Security | **Security-critical BL item descriptions (hook type, OAuth scope) must be validated before backlog registration — not caught at Reviewer approval**: task-048 code-quality-reviewer found M-1 (conf 88: BL-107 post-commit→PreToolUse) and M-2 (conf 85: BL-111 gmail.readonly scope missing) after Reviewer had already approved. Both required a Builder loop. Security/hook BL descriptions must specify: (a) hook type (PreToolUse/PostToolUse), (b) what is emitted on match (path+line only), (c) OAuth scope if applicable — before marking the item ready. | All future tasks registering Hook/MCP BL items involving credentials or OAuth |
| 2026-04-27 | code-quality-reviewer | **Fail-open with explicit logging is the correct safety pattern for budget enforcement — but silent swallowing of zero-budget intent is the inverse risk**: `or 10` fallbacks fire on `None` and `0` identically. When a user wants to disable enforcement (set budget to 0), the fallback silently re-enables it. Future CQR audits should flag this anti-pattern whenever config defaults and runtime fallbacks co-exist. | All future config-driven enforcement tasks |
| 2026-04-29 | Builder | **Correlated-regex pattern is required for multi-condition hooks — independent `re.search` scans produce false positives**: task-049 Hook 1 initially used two separate `re.search` calls (one for `re.DOTALL`, one for `$`) that could match in unrelated source locations. CQR caught this (conf 85) before shipping. The fix: a single regex that requires both `$` and `re.DOTALL` to appear inside the *same* `re.compile(...)` call. Whenever a hook rule requires two patterns to co-occur in a logical unit (function call, block, expression), use a single correlated regex — never two independent scans on the same content. | All future PreToolUse hook implementations with multi-condition rules |
| 2026-04-29 | Builder | **Write-only enforcement for file-content integrity hooks — Edit tool delivers diff fragments, not the full document**: Hook 3 (queue.json `status:done` check) initially operated on `new_string` from Edit calls. A valid two-step edit — Edit 1 sets `status: done`, Edit 2 sets `artefact_path` — was falsely blocked on Edit 1 because the fragment had no `artefact_path`. Fix: return `(None, None)` early for `Edit`/`MultiEdit` tool calls; enforce only on `Write` where the full document is available. Any hook that requires document-level invariants (e.g. "field A and field B must both be present") must operate on Write calls only. | All future workflow-guard and data-integrity hooks |
| 2026-04-29 | Builder | **Blocking rules must bypass dedup state — advisory-rule dedup silently suppresses repeated enforcement on the same file+rule**: task-049 CQR F3 (conf 82) caught that the session-dedup mechanism suppressed every repeat fire of a blocking rule after the first trigger. A second credential commit in the same session would have been allowed. Fix: add a `BLOCKING_RULE_NAMES` constant; blocking rules always fire regardless of `shown_warnings`. Dedup is correct for advisory rules (avoid noise) but is a security hole for blocking rules (removes the hard stop). | All future PreToolUse blocking hook implementations |
| 2026-04-29 | Builder | **Self-triggering hooks require an explanatory comment warning future editors**: Hook 1's own source contains a `re.compile(...re.DOTALL)` call and a `$` character, making future edits to that rule trigger itself. Fix: add a `# NOTE:` comment block immediately above the self-triggering rule directing editors to use the split-edit workaround documented in CLAUDE.md. Any hook rule whose source code contains the exact pattern it guards against must carry this comment. | All future security hook implementations |
| 2026-04-29 | Builder | **Security hook blocking messages must include file path and line number — matched text must never appear in output**: CQR F4 (conf 80) flagged that blocking output lacked file context, violating the CLAUDE.md policy "emit path+line only — never matched text." Fix: add a best-effort line-number scan (iterate `content.split("\n")`, check each line); prefix output with `[file_path:line_number]`. The "never matched text" rule is a leak-prevention constraint: printing the matched text in the terminal embeds the problematic content in shell history and logs. | All future PreToolUse hook blocking output |
| 2026-04-29 | Builder | **Hook pattern interference is real and must be tested before writing artefacts**: when writing build_notes.md and test files, task-049 Builder was blocked by the newly installed Hook 1 — the test payload strings (containing `re.DOTALL` + `$`) triggered the hook at write time. Fix: assemble test payloads via string concatenation at runtime; split artefact edits to use narrow `old_string` that avoids the trigger token. This is the second task (after task-047) to encounter hook self-interference. Rule: for any hook-installation task, test with benign payloads before writing documentation or tests that contain blocked pattern examples. | All future hook installation and testing tasks |
| 2026-04-29 | code-quality-reviewer | **CQR catches CLAUDE.md M-1 mirror violations for format strings missed by scoped Builder and Reviewer**: task-050 Builder correctly updated self-improver.yaml and lessons.md header but left the CLAUDE.md lessons format string (append-only table description) 2 columns short. Reviewer (scope-limited to the two changed files) did not flag it. CQR independently identified it as a Major M-1 violation. Whenever a format string is defined in both CLAUDE.md and an agent YAML, CQR must be included in the pipeline to verify all copies — not just Reviewer. | All future schema/format-change tasks | 85 | project_manager |
| 2026-04-29 | Builder | **Schema additions to an append-only table create structural inconsistency in existing rows — queue a backlog item at build time**: task-050 added 2 columns to lessons.md without backfilling existing rows (explicitly out of scope). Reviewer F1 (conf 72) and CQR m-2 both flagged that existing rows are now structurally ragged (4 fields vs 6). This inconsistency is a latent tooling bug if lessons.md is ever parsed by column index. Fix: when adding columns to an append-only table, queue a follow-up backlog item to backfill with placeholder values (`—`) before closing the task. | All future schema-extension tasks on append-only logs | 80 | project_manager |
| 2026-04-29 | SelfImprover | **Calibration rubrics defined in build_notes.md are invisible to the agent at runtime — embed rubrics directly in the agent YAML prompt**: task-050 design notes defined a confidence scoring rubric (90-100 proven / 70-89 high / 50-69 moderate / 1-49 speculative) in build_notes.md, but this rubric was not added to self-improver.yaml. The agent only reads its own prompt at runtime; build_notes.md is inaccessible. CQR m-1 and Reviewer F3 (conf 48) both flagged this gap. Any scoring or calibration rubric that governs an agent's output quality must be embedded in that agent's YAML prompt, not external docs. | All future agent YAML prompt designs | 78 | project_manager |
| 2026-04-30 | Builder | **Order early-exit checks before binary-presence checks in shell hooks — otherwise clean repos fail when the optional tool is absent**: task-051 Tester caught a bug where `ansible-lint` binary check ran before the empty-staged-files early exit. On a clean repo with no staged YAML files, the hook exited 1 with an installation error even though no lint was needed. Correct sequence: (1) collect inputs, (2) exit 0 if inputs empty, (3) check required tool present. Any script with optional-tool logic must follow this order. | All future shell hook and pre-commit script tasks | 92 | all |
| 2026-04-30 | Builder | **Under `set -euo pipefail`, capturing a command's exit code via `EXIT_CODE=$?` is dead code — the script terminates before the capture**: task-051 Reviewer F1 (confidence 90) found `EXIT_CODE=$?` after `ansible-lint` was unreachable. `set -e` terminates on non-zero exit before `$?` is assigned. Use `if ! command ...; then` instead — the `if !` idiom suppresses `set -e` for the tested command and keeps the failure branch reachable. Add this to the Builder pre-submission checklist for any script using `set -e`. | All future shell script tasks | 90 | all |
| 2026-04-30 | Builder | **YAML file extension filters must include both `.yml` AND `.yaml` unless one is explicitly excluded**: task-051 CQR caught that `grep '\.yml$'` silently skipped 3 `.yaml` files in CCAS repos. The Reviewer rated it conf 72 (below the ≥80 fix threshold); CQR surfaced it as a real gap. Default pattern for YAML file filters: `grep -E '\.(yml|yaml)$'`. If excluding one extension is intentional, document the reason in a comment. | All future shell hook and linting script tasks | 85 | all |
| 2026-04-30 | Builder | **Credential-detection regex patterns must use `\S+` or broad character classes — narrow ASCII subsets silently miss valid credentials**: task-052 Reviewer F1 (conf 85) caught `sap_rfc_passwd` using a narrow character class that missed RFC passwords with `.`, `/`, `:`. SAP RFC passwords, HDBUSERSTORE keys, and similar credentials routinely contain non-alphanumeric chars. Default to `\S+` for password value captures; narrow only when the credential format is fixed and documented (e.g. UUID, 40-hex-char hash). | All future credential-detection hook and security rule tasks | 88 | all |
| 2026-05-01 | Builder | **`yaml.safe_load()` raises `ConstructorError` on Home Assistant custom tags — register a wildcard multi-constructor before loading any HA YAML**: HA config files use `!secret`, `!include`, `!include_dir_list`, `!include_dir_merge_named`, `!env_var`, etc. These are unknown to PyYAML's SafeLoader by default and cause a `ConstructorError`. Fix: call `yaml.add_multi_constructor('', _ignore_unknown_tags, Loader=yaml.SafeLoader)` before any `yaml.safe_load*()` call. Without this, the hook blocks every valid HA YAML commit containing custom tags — a complete false-positive. | All future Builder tasks writing YAML validators for HA or DSL-extended YAML | 95 | all |
| 2026-05-01 | Builder | **`yaml.safe_load()` raises on multi-document YAML — use `list(yaml.safe_load_all(...))` for complete stream validation**: `yaml.safe_load()` raises `yaml.scanner.ScannerError` if the input contains multiple documents separated by `---`. HA automation files and template exports legitimately use `---`. `yaml.safe_load_all()` consumes all documents and still raises `YAMLError` on syntax errors — use `list(...)` to force evaluation of the entire stream, not just the first document. | All future Builder tasks producing YAML syntax validators | 92 | all |
| 2026-05-01 | Builder | **PyYAML is NOT Python stdlib — hooks that depend on it must include an explicit `import yaml` availability check**: `yaml` is the `python3-yaml` Debian package (PyPI: `pyyaml`), not part of the Python standard library. Pre-commit hooks claiming "no external dependencies" when they import `yaml` are factually incorrect. Add a binary-check-style guard: `if ! python3 -c "import yaml" &>/dev/null; then echo "ERROR: PyYAML not found — install with: sudo apt install python3-yaml" >&2; exit 1; fi`. | All future shell hook and pre-commit script tasks using Python packages | 90 | all |
| 2026-05-01 | Builder | **Pre-commit hooks must validate staged index content via `git show ":$file"`, not working-tree files**: reading the working-tree path in a pre-commit hook means an unstaged on-disk edit after `git add` causes either a false pass (hook sees the fixed on-disk version) or a false fail (hook sees a subsequent broken edit). Pipe `git show ":$file"` into the validator via stdin; pass the filename separately as a `sys.argv` argument for error messages only. Also replace `[ -f "$file" ]` existence guards with `git ls-files --error-unmatch "$file"` to check the index, not the disk. | All future Builder tasks producing pre-commit or pre-push hooks | 88 | all |
| 2026-05-01 | Tester | **`git show -- ":$file"` is WRONG for index-entry reads — do NOT add `--` before a `:filename` index refspec**: CQR recommended `--` (confidence 85) as flag-injection hardening for `git show ":$file"`. This is factually incorrect: `git show -- ":$file"` treats `:$file` as a working-tree pathspec and returns commit log/diff metadata instead of staged file contents, causing false-positive failures on every valid JSON commit. The `:filename` index-entry syntax is self-disambiguating — git cannot confuse `:file` with a flag. `--` is valid for separating flags from pathspecs in `git checkout -- <file>`, but changes semantics in `git show` with object references. Never add `--` before `:filename` index refspecs. | All future pre-commit hook tasks using `git show ":$file"` | 95 | all |
| 2026-05-01 | code-quality-reviewer | **CQR git-hardening recommendations must be verified against git documentation before applying — plausible patterns can be wrong for specific syntax contexts**: task-054 CQR recommended `git show -- ":$file"` (confidence 85, above loop threshold) as flag-injection hardening. The Reviewer accepted it; Builder applied it. Tester caught the resulting regression: all valid JSON commits were blocked (false positive). Static review and code review both missed the semantic change. Rule: any CQR recommendation that modifies a `git show`/`git diff`/`git ls-files` invocation must be verified against git man pages before applying — `--` has different effects across git subcommands and argument contexts. | All future Reviewer and CQR tasks touching git command hardening | 90 | all |
| 2026-05-01 | Tester | **Tester functional tests are the last line of defence against CQR recommendations that pass review but introduce regressions**: task-054 Reviewer and CQR both approved the `--` addition (CQR confidence 85, above the ≥80 fix threshold). The change was syntactically valid, conceptually reasonable, and passed `bash -n`. Only the Tester's functional test suite — staging a real file and running the hook — caught the false-positive regression. Never skip the Tester stage even when Reviewer+CQR are clean, and always include at least one end-to-end functional test (not just syntax check) for any pre-commit hook. | All future pipeline runs including pre-commit hook tasks | 92 | all |
| 2026-05-01 | Builder | **ruff stdin mode (`ruff check --stdin-filename "$file" -`) lints staged content without temp files — preferred over temp-file approach**: piping `git show ":$file"` directly into `ruff check --stdin-filename "$file" -` keeps the hook stateless, avoids temp-file cleanup, and correctly names the file in error output. This pattern is applicable to any linter that supports stdin (e.g. ruff, flake8 `--stdin-display-name`). Temp-file approaches require cleanup guards and can leave stale files on interrupted runs. | All future Builder tasks writing Python linting pre-commit hooks | 88 | all |
| 2026-05-01 | Tester | **`pip install ruff --break-system-packages` is the correct install path on Raspberry Pi OS system Python — document in test notes**: Raspberry Pi OS uses a PEP 668-enforced system Python that rejects `pip install` without `--break-system-packages`. When a pre-commit hook requires a non-stdlib binary (e.g. ruff, ansible-lint, shellcheck), the Tester must either (a) use `--break-system-packages` to install it, or (b) note "tool not pre-installed; install via `pip install <tool> --break-system-packages` or `sudo apt install <pkg>`" in the test report prerequisites. Silence on installation method leaves the next Tester stranded. | All future Tester runs on Raspberry Pi OS for hooks requiring non-stdlib binaries | 82 | all |
