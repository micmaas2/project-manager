# /pm-lessons — Recent Lessons

Read tasks/lessons.md. Print the last 10 table rows verbatim.
Skip the header rows (# heading and | Date | Agent | … column header and separator).
If fewer than 10 data rows exist, print all of them.

After printing, add one line:
`Suggested next: /pm-start  (apply lessons — begin session checklist)`
