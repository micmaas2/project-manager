# Improvement Proposals — task-012

Generated by: SelfImprover [Haiku]
Date: 2026-04-10
Task: task-012 — Fix MAS daily facts agent (7-day person dedup + birth-date enforcement)

---

## Proposal 1

**Target file**: `CLAUDE.md` — section "Workflow Orchestration", step 6 (always-on pipeline)

**Change**:

Add the following sentence to the Reviewer + code-quality-reviewer parallel stage description:

> Before approving any regex that uses `re.DOTALL`, Reviewer must verify whether the pattern also uses `$` as a terminator in a lazy quantifier. If so, a multi-line fixture (input with 3+ lines before the next field label) must be included in the Tester test suite — `re.DOTALL | re.MULTILINE` together with `$` silently truncates multi-line captures to the first line.

Concretely, add one sentence to the existing bullet that describes the `code-quality-reviewer` role:

```
- `code-quality-reviewer` — security, quality, best-practices check on all new/modified code;
  for any regex using re.DOTALL, flag use of $ as a stop anchor (use \Z instead) and require a multi-line fixture in Tester
```

**Rationale**:

In task-012, Reviewer (YAML) and code-quality-reviewer (built-in) both reviewed `_parse_llm_response` and approved it after loop 1. Neither flagged the `re.MULTILINE + re.DOTALL + $` interaction. Tester caught the bug only because a multi-line fixture was written for T-05. The combination is a known Python regex pitfall that causes silent data truncation — it appears whenever LLM output parsers use structured-field lookaheads. Making the check an explicit Reviewer responsibility closes the gap without adding a new pipeline stage.

**Status**: APPROVED
