# Improvement Proposals — task-018

Generated by SelfImprover after pipeline completion.
Task: mas_agent: Fix mas-frontend Docker healthcheck (BL-059)

---

## Proposal 1

**Target file**: CLAUDE.md (n8n Workflow Deployment section)
**Change**: Add a note: when rebuilding a specific service with `docker compose up -d --build <service>`, Docker also rebuilds services it depends_on — plan for the full dependency build time, not just the target service.

**Rationale**: `--build mas-frontend` also rebuilt `mas-backend` (a 5+ minute Python pip install on Pi4 ARM). The build took much longer than expected. This is predictable from the `depends_on` relationship — document it so future tasks can set realistic wait times.

**Status**: REQUIRES_HUMAN_APPROVAL

---

## Proposal 2

**Target file**: CLAUDE.md (n8n Workflow Deployment section or a new Pi4 operational notes section)
**Change**: Add: `sudo git -C /opt/mas -c user.name='Michel Maas' -c user.email='michel@femic.nl' <cmd>` — the /opt/mas git repo is owned by root; all git operations require sudo and explicit identity flags since root has no git config on Pi4.

**Rationale**: Two separate git operations failed (branch creation, merge commit) because root's git identity is unconfigured. The `-c` flag pattern avoids permanently configuring root's git and is repeatable.

**Status**: REQUIRES_HUMAN_APPROVAL
