# Improvement Proposals — task-013

Generated by: SelfImprover (Haiku 4.5)
Task: task-013 — /pm-plan skill (S-003-2)
Date: 2026-04-11

---

## Proposal 1

**Target file**: `.claude/commands/pm-plan.md`

**Change**: Add an explicit instruction in Step 8 to handle the case where the feature branch already exists (e.g. if a previous planning session was interrupted mid-commit). Add after the branch creation line:

```
If the branch `feature/pm-plan-<YYYY-MM-DD>` already exists locally, delete it first with:
`git branch -D feature/pm-plan-<YYYY-MM-DD>`
Then re-create from develop.
```

**Rationale**: If a planning session crashes or is interrupted after `git checkout -b` but before the merge, the next invocation on the same day will fail with "branch already exists". Making the recovery path explicit prevents a confusing failure mode.

**Status**: REQUIRES_HUMAN_APPROVAL

---

## Proposal 2

**Target file**: `tasks/lessons.md`

**Change**: Append row:

```
| 2026-04-11 | Builder | Skill files that commit to multiple tracking files (queue.json + backlog.md + kanban.md + epics.md) must bundle all writes into a single feature branch commit — partial commits leave tracking files out of sync if the session is interrupted. | task-013 |
```

**Rationale**: The Reviewer caught that queue.json was being written in Step 7 without being committed in Step 8. The fix was applied, but the general pattern — bundle all correlated writes into one commit — is worth recording as a lesson for future skill and agent development.

**Status**: REQUIRES_HUMAN_APPROVAL
