# Improvement Proposals — task-006

[Sonnet] Generated by SelfImprover on 2026-04-07.

---

## Proposal 1

**Target file**: `CLAUDE.md` — Workflow Orchestration section, PM Planning Session paragraph

**Change**:
Add an explicit preflight bullet to the PM Planning Session description:

```
**PM Planning Session**: invoke ProjectManager with "planning" intent to review backlog,
reprioritize, and onboard new projects. PM presents backlog and asks for user confirmation
before queuing tasks. **Preflight**: run `ls artefacts/` before assigning any task ID —
use a descriptive suffix (e.g. task-007-gate/) if the target path already exists.
```

**Rationale**:
The lesson "check for artefact directory conflicts" was added to CLAUDE.md's queue section and to lessons.md in the same session it was violated (task-006 and task-007 both needed suffixes). The fix is documented but not surfaced at the moment of action (planning). Making it a visible preflight bullet in the PM Planning Session paragraph increases the chance it is followed before a conflict is created. This is the same issue in 2 consecutive task assignments (task-006-lessons, task-007-gate), meeting the "2+ runs" threshold for a proposal.

**Status**: APPROVED

---

## Proposal 2 — REJECTED: already implemented in CLAUDE.md step 0b

**Target file**: `CLAUDE.md` — Workflow Orchestration section, step 0b

**Change**:
Extend step 0b to explicitly state that the lessons are used to inform _task design_ as well as planning approach:

Current:
```
0b. **Session start — read lessons (mandatory second action)**: Read `tasks/lessons.md`. Surface the **3 most recent rows** as context before making any planning or approach decisions this session. If the file is missing or has no data rows, skip and proceed.
```

Proposed addition (append to step 0b):
```
Lessons inform tooling choices, testing approach, and task design — do not repeat past mistakes captured here.
```

**Rationale**:
task-006's test report (T-04) confirmed lessons.md was consulted and influenced this session's decisions. Explicitly naming the three areas (tooling choices, testing approach, task design) makes the instruction more actionable and reduces the chance a PM session reads lessons.md but only applies it to high-level planning while missing concrete task design implications (e.g. prerequisites list, fixture strategy).

**Status**: REJECTED: already implemented — "Lessons inform tooling choices, testing approach, and task design" is already present in CLAUDE.md step 0b
