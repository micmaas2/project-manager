# Improvement Proposals — task-017

Generated by SelfImprover after pipeline completion.
Task: Cross-project kanban view script (BL-064, S-003-3)

---

## Proposal 1

**Target file**: CLAUDE.md
**Change**: Add `cross-kanban.py` to the Shell script pre-submission check section — explicitly note that Python scripts do not use `bash -n`; use `python3 -m py_compile <script>` for syntax checking instead.

**Rationale**: During task-017 Builder ran `bash -n scripts/cross-kanban.py` which failed with a syntax error (Python parens misread by bash). The correct check is `python3 -m py_compile`. Adding this note prevents the same mistake in future Python script tasks.

**Status**: APPROVED

---

## Proposal 2

**Target file**: CLAUDE.md (Agent Roles & Spawn Order → Built-in Claude Code agents)
**Change**: Note that Explore-type subagents cannot write files. When using the Agent tool with `subagent_type: Explore` for DocUpdater/docs-readme-writer roles, the parent agent must apply the edits directly after receiving the agent's proposed content.

**Rationale**: Both DocUpdater and docs-readme-writer Explore agents correctly identified the required changes but could not write files. The parent agent had to apply the edits. This is expected but not documented — future PM sessions may wait for the agent to write rather than applying the output themselves.

**Status**: APPROVED
