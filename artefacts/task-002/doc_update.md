# Doc Update — task-002

## Changes Made

### CHANGELOG.md — [Unreleased] section
Added entry for `artefacts/task-002/audit-summary.sh` describing:
- Purpose: audit.jsonl → grouped action summary
- Optional log file path argument
- jq >= 1.6 prerequisite
- 100% test pass rate (5/5)
- MVP2 validation note (preflight + fixtures improvements validated)

## No README changes required
The audit-summary.sh script is not a user-facing feature of the MAS system itself — it is an internal operational tool. README does not need updating.
