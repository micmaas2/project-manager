# Improvement Proposals — task-015

Generated by SelfImprover after task-015 (pm-priority.py, BL-004).

---

## Proposal 1

**Target file**: `tasks/lessons.md`

**Change**: Append the following row to the lessons table:

```
| 2026-04-11 | Tester | **Test fixture files must write inside the workspace root when a path-containment guard is active**: task-015 tests initially used `delete=False` `tempfile.NamedTemporaryFile` objects in `/tmp`, which the `_safe_path()` workspace-root guard rejected at runtime. Fix: all test fixture files must be written to a path inside `artefacts/<task-id>/` so the containment check passes. Add this as a standard note in any Tester prompt for scripts that accept `--queue`/`--backlog`-style path arguments. | All future Tester tasks for scripts with path-containment guards |
```

**Rationale**: The test suite required a non-obvious fixture-location adjustment (write inside the workspace, not `/tmp`) because the production path-containment guard also runs during tests. This pattern will recur for any script that validates its input paths against a workspace root. Capturing it in lessons prevents a future Tester from losing time to the same failure.

**Status**: REJECTED: duplicate of lesson already appended by task-016 SelfImprover

---

## Proposal 2

**Target file**: `CLAUDE.md`

**Change**: In the "Shell script pre-submission check" bullet list (Builder must verify before handing off to Reviewer), add the following item:

```
- If the script accepts file-path arguments (e.g. `--queue`, `--backlog`, `--config`): add a `_safe_path()` containment guard that resolves the path and asserts `_WORKSPACE_ROOT in path.parents` — exit 1 with a descriptive error if not. Document the workspace root as a module-level constant.
```

**Rationale**: task-015 introduced this guard only because code-quality-reviewer flagged path traversal as BLOCKING. The pattern (resolve → assert parents contains workspace root → exit 1 on failure) is generic and should be a standard pre-submission requirement for any script that accepts file paths via CLI, not something discovered at review time.

**Status**: APPROVED

---

## Proposal 3

**Target file**: `tasks/lessons.md`

**Change**: Append the following row to the lessons table:

```
| 2026-04-11 | Builder | **Include BL-NNN in every queue.json task title so priority can be looked up cross-referencing backlog.md**: pm-priority.py extracts BL-NNN from the task title via regex to look up priority in backlog.md. Tasks whose titles contain only a story ID (S-NNN) have no BL cross-reference and are ranked as lowest priority by default. Fix: when queuing a task, always include `(BL-NNN)` in the title. Consider adding a lint step to the PM planning skill that warns when a queued task title contains no BL-NNN. | All future PM planning sessions and pm-plan skill |
```

**Rationale**: task-015/016/017 were initially queued with only S-NNN story IDs, requiring a manual fix to insert BL-NNN before the priority script could rank them correctly. The BL-NNN is the single source of priority truth; tasks without it silently fall to the bottom of the ranked table. A conventions lesson prevents this recurring and flags the option to add a lint guard to the pm-plan skill.

**Status**: APPROVED
